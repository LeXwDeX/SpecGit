version: 1
required_checks:
  - Test (linux-bash)
  - Test (macos-bash)
  - Test (windows-pwsh)
  - Lint & Type Check
automation:
  merge: false
  close_issues: false
validation:
  titles: true
  labels: kind

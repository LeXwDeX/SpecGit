version: 1
language: en
required_checks:
  - Test (linux-bash)
  - Test (macos-bash)
  - Test (windows-pwsh)
  - Lint & Type Check
validation:
  titles: true
  labels: kind
automation:
  merge: false
  close_issues: false

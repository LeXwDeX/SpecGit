# CLI 参考

使用 `specgit --help` 与离线 `specgit --schema` 查看已安装版本的契约。机器输出使用 `--json`，结构化输入使用 `--input-file`。退出零表示当前操作成功，不代表交付完成。

完整命令、参数和诊断见[原生参考](https://github.com/LeXwDeX/SpecGit/blob/main/runtime/REFERENCE.md)。1.0 的 `finish`、`accept`、`bind`、`unbind` 已退役。

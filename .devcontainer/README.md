# Native development container

The container provides Node for repository verification, Git, GitHub CLI and
Rust tooling. Rustup selects the pinned toolchain when entering `runtime/`.
VS Code uses rust-analyzer. Open this repository with the Dev Containers extension
and choose Reopen in Container, then follow [runtime development](../runtime/README.md).
The container setup itself is not native three-platform acceptance evidence.

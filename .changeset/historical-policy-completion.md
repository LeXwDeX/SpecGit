---
"specgit": patch
---

Recover configured issue closure after squash, rebase and fast-forward merges using provider-backed target history and immutable Git evidence. Reject missing, mismatched or changed policy history, including changes later reverted, while preserving original-parent authorization for merge commits.

Preserve concurrent user edits to authoritative policy and local hooks during harness refresh. Align automation references and completed-delivery guidance with the actual CLI, and avoid stale source-head warnings on verified merged targets.

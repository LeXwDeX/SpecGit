---
'specgit': minor
---

Add opt-in automated delivery merging and issue closure, with an explicit yes/no
choice at initialization (default no), re-selection through `init --force`, a
configured target branch, and complete current-head CI/CD evidence before any merge.

Preserve repository identity in closing references, select authoritative GitLab
pipeline attempts and merged result anchors, preserve completed records on failed
replacement, report only evaluated acceptance gates as complete, reconcile bootstrap
comments across retries, and remove a native ESM command import cycle. Align the
generated agent workflow with the user's existing authorization and the new opt-in gate.

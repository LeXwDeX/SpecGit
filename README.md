# Native verification reuse acceptance

Isolated acceptance fixture for SpecGit issue #473. Uses committed source, a digest-pinned local runtime and the generated reuse workflow. The external acceptance harness uses that same unpublished runtime for this test. No merge into the product main branch is intended.

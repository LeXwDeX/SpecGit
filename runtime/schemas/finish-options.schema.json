{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SpecGit 2 native acceptance options",
  "description": "Normalized options for read-only finish. No merge or mutation option exists.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "request": { "type": ["integer", "null"], "minimum": 1 }
  }
}

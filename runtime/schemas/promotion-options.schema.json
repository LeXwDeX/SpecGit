{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SpecGit 2 native promotion options",
  "type": "object",
  "additionalProperties": false,
  "required": ["request"],
  "properties": {
    "request": {"type": "integer", "minimum": 1},
    "source_request": {"type": "array", "maxItems": 100, "items": {"type": "integer", "minimum": 1}}
  }
}
